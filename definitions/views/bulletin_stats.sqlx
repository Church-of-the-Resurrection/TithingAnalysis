config {
    type: "operations",
    schema: "Views",
    hasOutput: true
}

CREATE OR REPLACE VIEW
  ${self()} AS (
  SELECT
    *
  FROM
    ${ref("Views", "trailing_budget_stats")}
  CROSS JOIN
    ${ref("Views", "trailing_household_stats")} );
